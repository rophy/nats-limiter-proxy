bandwidth:
  default_local: 102400   # 100KB/s per proxy instance
  users:
    alice:
      local: 5242880      # 5MB/s per proxy instance
    bob:
      local: 2097152      # 2MB/s per proxy instance

# Local rate limiting only - no coordination
redis:
  enabled: false